# Catatan Harian — 2026-08-01 (Jumat)

Lanjutan implementasi pohon konvergensi material. Hari ini masih di Task 3
dari 11, karena review menemukan cacat Critical yang menyentuh desain, bukan
sekadar kode.

**Commit hari ini:** `56adf59`, `0c080f7`, `14e9223`, `b514661`, `351e080`, `c75ef32`

---

## 1. Cacat Critical: siklus tertutup menghasilkan pohon kosong

Reviewer Task 3 menelusuri tes `siklus_dihentikan` secara manual dan menemukan
lubang desain yang tidak terlihat dari membaca kode saja.

**Skenarionya:** order OA menghasilkan material A dan memakan B; order OB
menghasilkan B dan memakan A. Siklus tertutup, tanpa akar dari luar.

**Yang terjadi:** aturan "akar = material yang tidak dipakai order lain"
menyisihkan **kedua** kandidat. `rt_node` kosong, `descend( )` tidak pernah
dipanggil, dan penjaga siklus yang justru dirancang untuk kasus ini tidak
pernah dieksekusi.

**Kenapa ini serius bukan cuma soal tes:** SO nyata dengan data RESB melingkar
akan menampilkan halaman kosong yang tidak bisa dibedakan dari "SO ini memang
tidak punya order". Diam, tanpa petunjuk apa pun.

**Keputusan:** pohon tidak boleh kosong secara diam-diam. Bila ada order tapi
deteksi akar tidak menemukan satu pun, **semua** order pembuat diperlakukan
sebagai akar dan diberi catatan `akar tidak terdeteksi (kemungkinan siklus)`.
Penjaga siklus di `descend( )` yang menghentikan rekursinya.

Aturan ini diangkat jadi **Global Constraint** (`56adf59`) supaya mengikat
task-task berikutnya, bukan hanya Task 3.

## 2. Perbaikan Task 3 (`0c080f7`)

- Fallback akar diimplementasikan; blok pembentukan simpul diekstrak jadi
  `build_root( )` agar tidak menduplikasi kode antara jalur akar normal dan
  jalur fallback
- `siklus_dihentikan` diperbarui: kini menuntut pohon **tidak** kosong,
  ada baris ber-catatan fallback, ada baris ber-catatan `rekursi dihentikan`
  (membuktikan penjaga siklus benar-benar jalan), dan jumlah baris tetap wajar
- Tes baru `batas_kedalaman_dihormati` ditambahkan — `c_max_depth = 10`
  sebelumnya tidak punya cakupan tes sama sekali

## 3. Verifikasi positif dari reviewer

Layak dicatat karena bukan semua temuan review berupa kabar buruk: reviewer
memverifikasi bahwa tes `akar_di_satu_item_komponen_di_lain` (ditambahkan
kemarin di luar brief) **benar-benar gagal pada kode lama dan lulus pada kode
baru**. Artinya perbaikan kunci `(matnr, kdpos)` kemarin memang mengunci
sesuatu yang nyata, bukan tes hiasan.

## 4. UI `index2.htm` diganti mengikuti `prototype/`

Permintaan user: UI dibuat **sama persis** dengan folder `prototype/`
(210 baris HTML + 287 JS + 472 CSS). Setelah UI beres, baru lanjut
memperbaiki pengambilan & tampilan data.

**Yang berubah secara mendasar:**

| Aspek | Sebelum | Sesudah |
|---|---|---|
| Judul | Lintasan Routing & Confirmation | Dashboard Production |
| Sumbu filter | SO | **Buyer** |
| Pusat produksi | 3 section (Machining/Edge Banding/Assembling) | **2 center**: Machining Center (40 WC) + Edge Banding & Sanding (20 WC) |
| Alur | rantai stasiun | Storage → Machining → Edge Banding → Storage |
| Panel baru | — | Filter Buyer, daftar SO/PLO |

**Font Awesome tidak bisa dipakai.** Prototype memuatnya dari CDN; jaringan
SAP tertutup sehingga ikon tidak akan muncul sama sekali. Diganti 19
`symbol` SVG inline dengan nama yang mengikuti nama ikon FA aslinya, supaya
mudah dicocokkan dengan prototype. Hasil visual sama, sumbernya beda — ini
satu-satunya titik di mana "sama persis" tidak bisa dipenuhi secara harfiah.

**Yang dipertahankan:** blok DATA LIVE dibawa **byte-exact** dari versi
sebelumnya (disalin dari berkas lama, bukan diketik ulang), jadi kartu
KOMPONEN dan CONFIRMED tetap memakai query MSKA/AFRU yang sudah jalan.

**Yang masih dummy:** kartu BUYER / DI PRODUKSI / SELESAI PROD. / BOTTLENECK,
filter buyer, dua kotak center, peta work center, daftar SO/PLO, tabel
Detail Komponen. Semuanya dikirim ke JS sebagai array di blok bertanda
`DATA DUMMY UNTUK JS` — fase data nanti cukup mengganti isi array itu tanpa
menyentuh logika filter/render.

⚠️ **Catatan definisi yang belum beres:** prototype menulis CONFIRMED =
"basis komponen diterima". Angka yang kita hitung berbasis **operasi**
ter-konfirmasi (AFRU). Sub-teks kartu sengaja tetap menyebut "op" supaya
tidak mengklaim hal yang belum dihitung. Ini pekerjaan fase data.

## 5. Bug: tag HTML literal di komentar ABAP

Menulis `<script>` dan `<link>` utuh di dalam komentar ABAP membuat parser
HTML editor mengira ada blok script terbuka di situ, lalu memparse **seluruh
sisa berkas sebagai JavaScript** — ratusan diagnostik palsu.

Tidak merusak BSP (BSP hanya peduli delimiter `<%`/`%>`), tapi meracuni
tooling dan menyembunyikan error sungguhan. Kelas masalah yang sama dengan
bug delimiter kemarin: **hati-hati menulis sintaks bahasa lain di dalam
komentar.** Diperbaiki dengan menulisnya tanpa kurung sudut.

## 6. `scripts/check_bsp.py`

Pemeriksa struktur berkas BSP dijadikan skrip tetap, bukan perintah sekali
pakai. Memeriksa: delimiter nyasar di blok ABAP, pasangan `<% %>` /
`<%-- --%>` / `LOOP` / `IF` / `CASE`, keseimbangan `div` & `span`, dan tag
HTML literal di komentar ABAP.

Dua alarm palsu ditemukan dan diperbaiki saat mengujinya ke halaman lain:

- `TRANSLATE ... TO UPPER CASE` terhitung sebagai pernyataan `CASE`
- penyebutan tag di dalam komentar CSS terhitung sebagai tag sungguhan

**Batas metode yang didokumentasikan di skripnya:** hitungan div/span hanya
sahih untuk halaman yang markup-nya tidak dipecah lintas cabang ABAP.
`monitoring.htm` membuka `div` di satu cabang `IF` dan menutupnya di cabang
lain — hitungan mentahnya wajar tidak seimbang, dan itu **bukan** cacat.
Halaman itu diperiksa manual dan terbukti sehat.

## 7. UI dikonfirmasi jalan di SAP

**Dependensi aktivasi diperiksa dan hasilnya: tidak ada.** `index2.htm`
berdiri sendiri — tanpa link/AJAX ke halaman lain, tanpa panggilan class,
tanpa MIMEs, CSS/JS inline seluruhnya. Cukup aktifkan satu berkas itu.
Semua penyebutan `dash_cs.htm` / `diag_routing.htm` di dalamnya ada di
komentar ABAP, bukan pemanggilan. Tabel yang dibaca: MSKA, VBAK, AFKO⨝AFPO,
AFVC, AFVV, AFRU.


User mengaktifkan `index2.htm` di SE80. **Tampilannya sudah sesuai** — ini
konfirmasi pertama bahwa pengganti Font Awesome (19 `symbol` SVG inline)
memang tampil di browser SAP, sesuatu yang sebelumnya hanya asumsi.
Perbaikan data dikerjakan bertahap setelah ini.

## 8. Penataan repo: cermin SAP vs bahan rujukan

Masalah yang dilaporkan user: sistem sering direvisi dan berkasnya
tercampur, sulit fokus ke pengembangan yang sedang berjalan.

Akar masalahnya bukan sekadar berantakan — ada **lima** tempat berisi berkas
mirip dan **tidak ada penanda mana yang benar-benar hidup di SAP**, sehingga
tiap sesi harus menebak lagi.

Ditetapkan satu aturan yang menentukan segalanya:

> `ZBSP_CS_APP/` adalah **cermin objek SAP**. Apa pun di dalamnya ada, atau
> akan ada, sebagai objek di sistem. Apa pun di `reference/` **tidak**
> diaktifkan.

Pemindahan yang dilakukan:

| Dari | Ke |
|---|---|
| `ZBSP_CS_APP/backup/` | `reference/backup-pages/` |
| `ZBSP_CS_APP_V2/` | `reference/v2-so-tracer/` |
| `prototype/` | `reference/prototype-ui/` |
| `Page with Flow Logic/prototype.html` | `reference/prototype-ui/prototype-lama.html` |

`prototype.html` adalah mockup, bukan halaman BSP. Menaruhnya di folder
halaman membuat orang mengira ada halaman yang belum diaktifkan — persis
kebingungan yang mau dihilangkan.

Rujukan path di komentar `diag_routing.htm`, `index2.htm`, `routing_map.htm`
ikut diperbarui supaya tidak menunjuk lokasi lama. `README.md` ditulis ulang
memuat aturan folder, isi `reference/`, cara pakai `check_bsp.py`, urutan
aktivasi class-sebelum-halaman, dan batasan jaringan tertutup — isi lamanya
sudah jauh tertinggal, masih menyebut aplikasi hanya punya 3 halaman.

`ZBSP_CS_APP_V2/` **dipulihkan lebih dulu** sebelum dipindah — penghapusannya
kemarin tidak disengaja.

## 9. Roadmap fase data `index2.htm`

Dibuat `report/ROADMAP-index2-data.md` sebagai pegangan penyambungan data,
dikerjakan tahap demi tahap.

**Urutan tahap** (dari yang paling kokoh dasarnya ke yang paling perlu
keputusan):

| Tahap | Panel | Sumber utama |
|---|---|---|
| 1 | Filter Buyer + kartu BUYER | pola `dash_cs.htm`: MSKA 2KCS → VBAK-KUNNR → KNA1-NAME1 |
| 2 | Daftar SO/PLO | `lt_stok` + `PLAF-PLNUM` |
| 3 | Dua kotak center | order per DISPO + rumus status operasi |
| 4 | Tabel Detail Komponen | komponen dalam cakupan + AFVC/CRHD |
| 5 | Peta Work Center | AFVC-ARBID → CRHD/CRTX |
| 6 | Kartu DI PRODUKSI / SELESAI / BOTTLENECK | hasil tahap 3 & 5 |
| 7 | Finalisasi definisi CONFIRMED | setelah data komponen ada |

**Tiga ketidakcocokan prototype vs data nyata** dicatat di muka supaya tidak
dikira bug saat muncul nanti:

- prototype mengasumsikan 60 WC; kenyataannya **17 WC** pada scope SO 10446
- "Edge Banding & Sanding" digabung di prototype, tapi hanya `EB2` yang punya
  DISPO — **Sanding tidak punya order sama sekali**, cuma stok di `229K`
- kolom QTY ROUTING tidak akan pernah menampilkan rantai tahap, karena rantai
  itu tidak ada di level operasi

**Enam keputusan** sengaja ditunda sampai tahapnya tiba, bukan diputuskan
sekarang — supaya berdasar angka nyata yang sudah terlihat, bukan tebakan:
arti `%` per buyer, arti 4 segmen bar SO, arti "WIP" di kotak center, isi
kolom QTY ROUTING, bentuk peta WC, dan definisi CONFIRMED.

**Ambang kinerja ditetapkan:** kalau waktu muat melewati 5 detik, penambahan
query dihentikan dan dipindah ke pola AJAX + cache seperti `dash_cs.htm`.
`index.htm` dulu ber-TTFB 15 detik karena query stok dijalankan sinkron —
jangan diulangi.

## 10. Ketentuan wajib dari user — roadmap direvisi

User menetapkan ketentuan yang mengubah beberapa hal mendasar. Roadmap
ditulis ulang mengikutinya.

**W1 — cakupan diperluas jadi enam SLoc:** `2KCS`, `2261`, `2262`, `22E2`,
`22E3`, `229K`. Bukan lagi 2KCS saja. Berlaku untuk buyer, komponen, SO,
work center — semuanya.

> Konsekuensi yang harus diingat: kode sekarang masih memfilter `2KCS` saja,
> jadi **angka kartu KOMPONEN akan naik** setelah tahap 0. Itu wajar, bukan
> regresi. Catat angka sebelum & sesudah.

**W2 — PLO tidak dipakai.** Keterangan PLO dihapus dari kartu BUYER dan panel
SO. `PLAF` tidak disentuh sama sekali di halaman ini.

**W3 — pemetaan center:** Machining Center = `2KCS`/`2261`/`2262`;
Edge Banding & Sanding = `22E2`/`22E3`/`229K`.

**Arti tiga warna Lintasan Produksi ditetapkan** (kuning → biru → hijau):

| Warna | Machining Center | Edge Banding & Sanding |
|---|---|---|
| 🟡 antri | masih di `2KCS` | belum masuk EB maupun sanding |
| 🔵 dikerjakan | ada di `2261`/`2262`; perlu dibedakan masih antre di center atau sudah masuk proses, dan di WC mana | ada di `22E2`/`22E3`/`229K`, info WC wajib |
| 🟢 sudah lewat | pernah masuk & selesai walau stok habis | idem; di fase ini barang siap pre-assy/assy |

**Permintaan baru di panel SO:** tampilkan semua SO + SO Item dengan urutan
yang tidak berantakan, tambah **kotak pencarian SO/Item**, dan **klik baris
membuka sidebar kanan** berisi seluruh komponen SO+Item itu sesuai enam SLoc.
Dua elemen terakhir belum ada di prototype.

**Peta Work Center:** tampil sesuai card yang aktif, semua WC ditampilkan,
dibedakan warna beban, dan diberi **tooltip modern** berisi isi WC tersebut.

## 11. Penghambat utama: warna hijau "sudah lewat"

Masalah yang user sampaikan sendiri: material yang sudah selesai diproses
otomatis tidak punya stok lagi karena lanjut ke proses berikutnya — tapi
halaman tetap harus bisa menyatakan ia **pernah masuk dan pernah selesai** di
center itu. Dan itu tidak berlaku bagi material yang memang tidak pernah masuk.

**Arah yang sudah terlihat:** saldo stok menjawab "di mana barang sekarang";
yang dibutuhkan warna hijau adalah "pernah lewat mana" — itu pertanyaan
**pergerakan barang**, bukan saldo. Sumbernya `MSEG`.

**Temuan saat menelusuri:** `ZCL_CS_UTIL` ternyata **sudah memuat mesin yang
merumuskan persis kebutuhan ini** — `cp_qty( )` menghitung qty net per
checkpoint dari `MSEG⨝MKPF`:

| Checkpoint | Definisi |
|---|---|
| CP1 | net masuk `2KCS` dari Plant 1000 (301/311 'S'), dikurangi 'H' balik |
| CP2 | net masuk `2261` dengan `umlgo=2KCS` |
| CP3 | GR bwart 101 di `2262` |
| CP4 | net masuk `229K` dari sumber mana pun, dikurangi 'H' keluar |

Aturan globalnya: bwart `321` selalu dikecualikan — itu pelepasan Quality
Inspection, bukan perpindahan fisik.

⚠️ **Tapi mesin itu kode mati.** Diperiksa hari ini: `cp_qty`,
`item_cp_status`, dan `dot_stages` **nol pemanggil**. Rumusannya matang tapi
belum pernah terbukti terhadap data nyata — jangan langsung dipakai.
Yang menenangkan: empat halaman aktif (`dash_detail`, `dash_feed`,
`dash_mrp`, `monitoring`) sudah membaca MSEG langsung, jadi pendekatannya
sendiri terbukti jalan di sistem ini.

Lima hal yang harus diputuskan saat brainstorming K3 tercatat di roadmap.

## 12. Dokumentasi

Dibuat tiga berkas catatan:

- `ZBSP_CS_APP/CHECKPOINT.md` — ringkasan status seluruh jalur pengembangan
- `ZBSP_CS_APP/report/daily/2026-07-31.md` — catatan harian kemarin
- `ZBSP_CS_APP/report/daily/2026-08-01.md` — berkas ini

Berkas checkpoint & harian ini diperbarui lagi setelah perubahan UI.

---

## Status Task pohon konvergensi

| Task | Isi | Status |
|---|---|---|
| 1 | Kerangka class, `op_status( )`, `stn_of_order( )` | ✅ Selesai |
| 2 | Sisi pohon & deteksi akar | ✅ Selesai |
| 3 | DFS berlevel, siklus, duplikat | 🔧 Perbaikan selesai (`0c080f7`), re-review **tertunda** — loop SDD dijeda untuk mengerjakan perubahan UI |
| 4–11 | Status kesiapan, `build( )`, perombakan halaman, `index2.htm` | ⬜ Belum |

## Belum terverifikasi

Masih sama dengan kemarin dan ini yang paling penting diingat:
**`ZCL_CS_PEG` belum pernah dibuat di SE24, belum diaktifkan, dan kesepuluh
test ABAP Unit belum pernah dijalankan.** Seluruh temuan sejauh ini didapat
dari penelusuran logika manual, bukan dari eksekusi.

Aktivasi + eksekusi tes dijadwalkan setelah Task 6.

## Selesai ditindaklanjuti

Penghapusan `ZBSP_CS_APP_V2/` yang sempat terdeteksi di working tree ternyata
**tidak disengaja** — sudah dipulihkan (`git checkout`), lalu dipindah rapi ke
`reference/v2-so-tracer/`. Folder `ZBSP_CS_APP copy/` (2 berkas PNG duplikat)
sudah tidak ada lagi saat penataan dikerjakan.
