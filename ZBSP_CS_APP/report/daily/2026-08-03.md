# Catatan Harian — 2026-08-03 (Minggu)

Fase data `index2.htm` dilanjutkan. Dua pekerjaan: **tabel Detail Komponen
diaktifkan** (tahap 5 roadmap) dan **paket Perbaikan #1** — header + tiga kartu
ringkasan.

**Commit hari ini:** `6d30cfa`
**Belum di-commit:** paket Perbaikan #1 (`index2.htm`, `dash_prod.htm`)

Tidak ada catatan harian 2026-08-02.

---

## 1. 🔴 Temuan paling penting: seluruh JavaScript `index2.htm` mati

Ditemukan saat menyiapkan pratinjau browser, bukan dari membaca kode.

`drawCenters()` menulis atribut tooltip seperti ini:

```js
'<span class='ct-h'>WIP &mdash; komponen sedang di center ini</span>' +
```

Tanda kutip tunggal di dalam string berkutip tunggal. Ini **bukan** sekadar
salah tampil — ini *syntax error*, dan satu syntax error menggugurkan
**seluruh blok `<script>`**. Artinya sejak `2752c18` (2026-08-01, "potongan 3")
tidak ada satu pun panel `index2.htm` yang terisi: filter buyer kosong, dua
kotak center kosong, peta WC kosong, daftar SO kosong.

Diperbaiki dengan kutip ganda. Tanpa ini, apa pun yang ditambahkan hari ini
juga tidak akan pernah jalan.

**Pelajaran yang layak dibawa:** tiga commit berturut-turut ("potongan 1, 2, 3")
diserahkan tanpa halaman itu pernah benar-benar dibuka. Sejak hari ini
`index2.htm` diuji lewat tiruan browser + endpoint palsu sebelum diserahkan —
prosedurnya ada di bagian 6.

**Ikutannya:** `selectBuyer()` menyimpan blok yang menimpa `mc-wip-text` /
`ebs-wip-text` dengan angka tetap (`213`/`167` dan `34`/`27`) untuk buyer
contoh `nordic` / `westfield`. Setelah `BUYERS` diisi KUNNR nyata, cabang
`westfield` tidak pernah tercapai sehingga cabang lain **selalu** jalan dan
menimpa angka asli `dash_prod.htm` dengan angka karangan. Dibuang, diganti
`drawCenters()`.

---

## 2. Tabel Detail Komponen aktif — tahap 5 roadmap (`6d30cfa`)

### Keputusan yang diambil user

| # | Keputusan |
|---|---|
| D1 | Status komponen dari **konfirmasi operasi (AFRU)**, bukan posisi SLoc |
| D2 | Satu baris = satu **komponen**, bisa dibentangkan |
| D3 | Isi baris bentang = **TAHAP (order)**, bukan VORNR |
| D4 | Muat ringkasan dulu (400 komponen), rincian saat dibentangkan |
| D5 | Sales Order + Detail Komponen jadi satu baris, 1fr : 2fr, tinggi 560px |
| D6 | Tab default **Dikerjakan** |

### Temuan yang mengubah desain di tengah jalan

`diag_routing.htm` bagian B — dicatat di `CHECKPOINT.md:67` dan
`routing_map.htm:12-19` — sudah lama menyimpan jawabannya:

> Maksimum 2 operasi per order; 86,1% order hanya punya 1 operasi; tidak ada
> yang ≥ 3. Alur Machining → Edge Banding **bukan** routing multi-operasi
> dalam satu order, melainkan **rangkaian order terpisah**.

Dua akibat langsung:

- Baris bentang berisi VORNR akan berisi satu baris saja pada 86% kasus —
  tidak berguna. Diisi **tahap**, ia jadi peta perjalanan komponen. (D3)
- Pemetaan komponen ke center **tidak perlu menebak dari nama work center**.
  `AFPO-PWERK` + `AFKO-DISPO` sudah menyatakannya — konvensi yang sama sudah
  dipakai `routing_map.htm:31-33`:

| seq | Tahap | PWERK | DISPO |
|---|---|---|---|
| 1 | Pembahanan | 1000 | WM1, WM2, PN1, PN2 |
| 2 | **Machining Center** | 2000 | GA1, GA2 |
| 3 | **Edge Banding** | 2000 | EB2 |
| 0 | di luar ketiganya | — | — |

⚠️ **Sanding (`229K`) tidak punya DISPO** — tidak ada order yang memetakan ke
sana. Pada tabel ini tombol "Edge Banding & Sanding" berarti **EB2 saja**.
Dinyatakan di kaki tabel, bukan didiamkan.

### Endpoint baru di `dash_prod.htm`

```
?part=komp   ctr=MC|EB · so= · item= · bid=
             -> {"ok":1,"more":n,"nook":n,"ctr":"..","rows":[
                  {so,item,mat,txt,bid,buyer,st,done,tot,wc,pct}],"ms":n}
             batas 400 komponen tanpa scope, 2000 bila discope
             cache DPRODKPM / DPRODKPE, hanya untuk permintaan TANPA scope

?part=ops1   so= item= mat=
             -> {"ok":1,"rows":[{seq,stg,aufnr,op,wc,st,done,need}],"ms":n}
             tanpa cache — kecil, hanya diminta saat satu baris dibentangkan
```

Keduanya membaca daftar komponen dari SHARED BUFFER `DPRODKM` yang sudah
ditulis `part=stock` — MSKA tidak ditembak untuk kedua kalinya. Bila `DPRODKM`
belum ada, dijawab `{"ok":0,"msg":"jalankan part=stock lebih dulu"}` dan UI
mencoba ulang sekali; bukan daftar kosong yang terbaca "tidak ada data".

### Pembagian penyaring — sengaja tidak di satu tempat

| Filter | Di mana | Alasan |
|---|---|---|
| center MC/EB | server | menentukan tahap mana yang dinilai |
| SO + Item | server | agar lepas dari batas 400 |
| buyer | server | agar lepas dari batas 400 |
| tab status | browser | instan |
| kotak cari | browser | instan |

Kalau SO disaring di browser, memilih SO di urutan ke-900 akan tampak kosong
padahal datanya ada — hanya 400 pertama yang dimuat.

### ⚠️ Angkanya sengaja TIDAK cocok dengan dua kotak center

Kotak center berbasis **saldo stok MSKA**; tabel ini berbasis **konfirmasi
operasi AFRU**. Dua pertanyaan berbeda. Peringatan ini ditulis di tooltip judul
tabel dan di kaki tabel supaya selisihnya tidak dikira bug — **jangan
"diperbaiki" agar sama**.

---

## 3. Perbaikan #1 — header & kartu ringkasan (belum di-commit)

Sumber: `perbaikan/perbaikan1.md`.

### 3.1 Header jadi bar biru penuh

`body` padding dinolkan, `<header>` dikeluarkan dari `.dashboard-container`,
sisanya dibungkus `<main class="content">`. Background `#1e40af`, teks putih,
dot status dinaikkan ke `#4ade80` karena `--color-green` (`#10b981`) terlalu
gelap di atas biru.

### 3.2 CONFIRMED → **OPERASI SELESAI**

Definisi & query **tidak berubah** — yang dihitung memang operasi. Yang berubah
hanya kejujuran labelnya. Sub-teks jadi `3.011 dari 5.280 operasi`; peringatan
pembatasan 1.500 order pindah dari sub-teks ke ikon ⚠ + tooltip.

**Ini menutup K6** dengan cara yang tidak diduga roadmap: bukan dengan memilih
salah satu definisi, tapi dengan mengakui apa yang sebenarnya dihitung. Kartu
tidak lagi mengklaim "komponen diterima".

### 3.3 DI PRODUKSI — sub-teks dua baris

`dari 4.820 komp (54%)` lalu `MC: 1.340 · EB: 780`. Seluruh angkanya sudah
dikirim `part=stock` sejak dulu (`komp`, `prod`, `mcp`, `ebp`) — hanya belum
dipakai layout. **Tidak ada query baru.**

### 3.4 SELESAI PROD. — redefinisi (Opsi B / Tafsir X)

Definisi lama adalah proxy sementara: "stok ada di `229K`". Definisi baru:

> komponen selesai = `AFPO-WEMNG ≥ AFPO-PSMNG` **DAN** tidak ada saldo di
> keenam SLoc cakupan.

Satu `SELECT AFKO⨝AFPO` baru (DISPO GA1/GA2/EB2), disaring dengan `lt_stok`
yang sudah in-memory. Dikirim sebagai field **`done_real`**.

Field lama `done` **sengaja dipertahankan** di JSON supaya angka lama & baru
bisa dibandingkan sampai PPIC memverifikasi. Hapus hanya setelah itu.

---

## 4. Dua kali menyimpang dari spec — keduanya karena spec-nya tidak bisa jalan

**a. Tooltip `trunc`.** Spec menaruh `<span id='tip-conf-trunc' style='display:none'>`
di **dalam** atribut `data-tip`, lalu menyembunyikan/menampilkannya lewat
`document.getElementById(...).style.display`. Tidak bisa bekerja: isi `data-tip`
hanyalah teks atribut, elemennya tidak pernah ada di DOM. Diganti — JS
**menambahkan** baris peringatan ke atribut `data-tip` saat `trunc=1`, pola yang
sama sudah dipakai `drawCenters()`.

**b. Deduplikasi di blok SELESAI PROD.** Satu komponen bisa punya beberapa
order (satu per tahap — lihat bagian 2). Tanpa
`DELETE ADJACENT DUPLICATES ... COMPARING kdauf kdpos matnr`, komponen yang
punya order Machining **dan** Edge Banding dihitung dua kali dan `done_real`
menggelembung. Spec tidak menyebutkannya.

⚠️ Ikutan dari (b) yang perlu diputuskan user: setelah deduplikasi, "delivered
penuh" berarti **setidaknya satu** order komponen itu terpenuhi. Digabung
syarat stok kosong di enam SLoc ini sudah ketat, tapi kalau maksudnya "semua
order-nya", ubah di satu tempat itu saja.

Penyimpangan ketiga yang lebih ringan: `.content` hanya diberi `padding`, bukan
`flex-direction: column; gap: 16px` seperti spec — anaknya cuma
`.dashboard-container` yang sudah melakukan persis itu.

---

## 5. Yang diverifikasi dan yang belum

**Diverifikasi lokal** (tiruan `index2.htm` + endpoint palsu, dibuka di Chrome):

- Klik kartu SO → kartu tersorot, tabel menyempit ke SO+Item itu, cacah tab
  ikut berubah, pita "sisa tidak dimuat" hilang
- Klik baris komponen → membentang jadi tiga tahap dengan WC, status, qty
- Ganti center MC ↔ EBS → tabel diambil ulang, kaki tabel berganti tahap dan
  memunculkan catatan Sanding
- Klik ulang kartu SO → pilihan lepas, daftar penuh kembali
- Header biru penuh, `57 % ⚠`, `3.011 dari 5.280 operasi`,
  `dari 4.820 komp (54%)` / `MC: 1.340 · EB: 780`,
  `1.187` / `komponen keluar dari 6 SLoc`
- Baris peringatan `trunc` benar-benar masuk ke atribut `data-tip`

**Diperiksa statis:** blok `<script>` terparse penuh; `<main>`/`<header>`/
`<div>`/`<section>` seimbang; ABAP IF/ENDIF 101/101, LOOP/ENDLOOP 34/34, tag
BSP 13/13, tanpa tabrakan nama TYPES/DATA/field-symbol.

**BELUM diverifikasi — butuh server SAP:**

- `part=komp` dan `part=ops1` belum pernah dijalankan di SE80
- Waktu jalan `SELECT` baru `done_real` belum diukur
- Angka `done_real` belum dibandingkan dengan `done` lama (sanity check PPIC)
- Cache lama belum dibersihkan dengan `?fresh=1`

Pesan commit `6d30cfa` menyebut verifikasi di server SAP sudah dilakukan; kalau
benar, empat butir di atas tinggal butuh konfirmasi angkanya — kecuali
`done_real`, yang memang baru dan belum di-commit.

---

## 6. Prosedur pratinjau `index2.htm` tanpa server SAP

Dibuat hari ini setelah temuan bagian 1. Skripnya ada di scratchpad sesi (tidak
masuk repo), tapi caranya cukup singkat untuk ditulis ulang kapan saja:

1. Baca `index2.htm`, buang seluruh tag `<% %>`, `<%= %>`, `<%-- --%>`
2. Sisipkan `<script>` yang menimpa `window.kpiGet` dengan stub pembalas JSON
   palsu **berbentuk sama persis** dengan yang dijanjikan `dash_prod.htm`
3. Sajikan lewat `http://localhost` (bukan `file://` — ekstensi browser menolak)
4. Cek juga secara statis: `new Function(isiBlokScript)` untuk menangkap syntax
   error seperti bagian 1

---

## Status fase data `index2.htm`

| Tahap | Isi | Status |
|---|---|---|
| 0 | Perluasan cakupan ke enam SLoc | ✅ Selesai (2026-08-01) |
| 1 | Filter Buyer + kartu BUYER | ✅ Selesai |
| 2 | Daftar SO + pencarian | ✅ Selesai; klik SO ditambahkan hari ini |
| 3 | Dua kotak center + tiga warna | ✅ Selesai — hijau dari `MSEG` (`2752c18`) |
| 4 | Peta Work Center | ⬜ **Belum** — masih `Math.random()` |
| 5 | Detail Komponen | ✅ Selesai hari ini (`6d30cfa`) |
| 6 | DI PRODUKSI / SELESAI PROD. / BOTTLENECK | 🟡 Dua selesai, BOTTLENECK masih dummy |
| 7 | Finalisasi CONFIRMED | ✅ Ditutup lewat rename OPERASI SELESAI |

## Keputusan terbuka — perubahan hari ini

| # | Keputusan | Status |
|---|---|---|
| K1 | Arti `%` per buyer | ✅ Ditutup 2026-08-01 |
| K2 | Arti segmen progress bar SO | ✅ Ditutup 2026-08-01 |
| K3 | Cara menghitung warna hijau "sudah lewat" | ✅ Ditutup 2026-08-01 (`MSEG`) |
| K4 | Isi kolom QTY ROUTING | ✅ **Ditutup hari ini** — jadi kolom OPERASI: `done/tot op` + work center aktif |
| K5 | Dasar perhitungan "beban" WC | ❓ **Masih terbuka** — memblokir tahap 4 & BOTTLENECK |
| K6 | Definisi CONFIRMED | ✅ **Ditutup hari ini** — label diubah jadi OPERASI SELESAI, definisi tetap operasi |

**K5 kini satu-satunya keputusan yang tersisa** dan ia memblokir dua hal
sekaligus: peta Work Center (tahap 4) dan kartu BOTTLENECK (tahap 6).

## Belum terverifikasi

`ZCL_CS_PEG` masih sama dengan 2026-08-01: **belum pernah dibuat di SE24,
belum diaktifkan, kesepuluh test ABAP Unit belum pernah dijalankan.** Task 3
re-review masih tertunda. Jalur itu tidak disentuh hari ini.
